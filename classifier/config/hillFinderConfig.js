export default {
	falseFlatDistance: 50, // the max distance in meters for a false flat
	hillGap: 200, // the max gap in meters between hills to combine them
	minGradient: 0.02, // the minimum gradient to be considered a hill
};
