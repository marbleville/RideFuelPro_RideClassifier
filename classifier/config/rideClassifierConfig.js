export default {
	minTotalIntervalTime: 300, // the minimum total time in seconds for intervals to be considered a interval ride
	enduranceFTPThresholdCoefficient: 0.75, // the coefficient to multiply the FTP by to determine the threshold for an endurance ride
	minAverageIntervalTime: 300, // the minimum average time in seconds to differentiate and interval ride form a race
	restFTPThresholdCoefficient: 0.5, // the coefficient to multiply the FTP by to determine the threshold for a rest ride
};
